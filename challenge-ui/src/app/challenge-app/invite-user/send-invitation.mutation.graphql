mutation sendInvitation(
  $forUserId: UserId!
  $fromUserId: UserId!
  $challengeId: ChallengeId!
) {
  sendInvitation(
    input: {
      forUserId: $forUserId
      fromUserId: $fromUserId
      toChallengeId: $challengeId
    }
  ) {
    id
  }
}
