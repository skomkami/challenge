query userChallenges($userId: UserId!, $size: Int, $offset: Int) {
  user(id: $userId) {
    id
    challenges(size: $size, offset: $offset) {
      challenge {
        name
        finishesOn
      }
      lastActive
      summaryValue
    }
  }
}
